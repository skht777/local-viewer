# コンテンツ親ディレクトリ (読み取り専用でコンテナの /mnt-host にマウント)
# この配下のサブディレクトリを manage_mounts.py でマウントポイントとして登録
DATA_DIR=/path/to/your/media

# ホスト側ポート
HOST_PORT=8000

# バインドアドレス: 127.0.0.1 (ローカルのみ) / 0.0.0.0 (LAN 公開)
BIND_HOST=127.0.0.1

# リソース制限
MEM_LIMIT=2g
CPUS=2.0
DISK_CACHE_SIZE=2g
