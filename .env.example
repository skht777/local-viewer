# Host directory to serve (read-only mount at /data inside container)
DATA_DIR=/path/to/your/media

# Port exposed on host
HOST_PORT=8000

# Bind address: 127.0.0.1 (local only) or 0.0.0.0 (LAN accessible)
BIND_HOST=127.0.0.1

# Resource limits
MEM_LIMIT=2g
CPUS=2.0

# Search / Index
INDEX_DB_PATH=/tmp/viewer-cache/index.db
WATCH_MODE=auto
WATCH_POLL_INTERVAL=30
REBUILD_RATE_LIMIT_SECONDS=60
SEARCH_MAX_RESULTS=200
SEARCH_QUERY_TIMEOUT=5
